#!/bin/bash

set -euo pipefail

function main() {
    case "${1}" in
        reload)
            # reload configs
            tmux source "${HOME}/.tmux.conf"
            ;;

        *)
            cd "${HOME}/.dotfiles" && git "$@"
            ;;
    esac
}

main "$@"
